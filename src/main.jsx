import React from 'react'
import ReactDOM from 'react-dom/client'
import SleeveCloudOS from '../sleeve-cloud-hub.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <SleeveCloudOS />
    </React.StrictMode>,
)
